import { useState } from "react"
//je moet hier een handle click maken, in de handle click functie moet je zorgen dat als je op die knop drukt 
function Fahrenheit(){
    const []=useState(change, setChange)
    handleClick = (e) =>{
        e.preventDefault


    }
    return(<>

    <input type="text" name="fahrenheit"/> <button onClick={handleClick}>berekenen</button>

    
    </>)
}

export default Fahrenheit